# === ADMIN CREDENCIALES (para inicialización) ===
ADMIN_EMAIL=admin@local
ADMIN_PASSWORD=Admin123!
ADMIN_NAME=Admin Interno

# === IA CONFIGURATION ===
# Elige uno: openai | anthropic | mock (default: mock)
# AI_PROVIDER=mock
# AI_API_URL=https://api.openai.com/v1/chat/completions  # Para OpenAI
# AI_API_KEY=tu_api_key_aqui
# AI_MODEL=gpt-4  # O el modelo que uses

# === SERVIDOR ===
# PORT=3000  # Vercel asigna automáticamente
# NODE_ENV=production

# === SEGURIDAD ===
# Session secret (cámbialo en producción!)
# SESSION_SECRET=tu-secreto-super-seguro-aqui

# === BASE DE DATOS ===
# Normalmente no necesita configuración (SQLite local)
# DB_PATH=./data/database.sqlite3
