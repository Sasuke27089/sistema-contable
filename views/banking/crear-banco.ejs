<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Crear Banco - Sistema Contable</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 text-gray-800 min-h-screen">
    <!-- HEADER -->
    <header class="bg-white shadow border-b border-gray-200">
      <div class="max-w-5xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Registrar Nuevo Banco</h1>
            <p class="text-sm text-gray-600">Agrega una nueva cuenta bancaria al sistema</p>
          </div>
          <a href="/banking" class="text-indigo-600 hover:text-indigo-700 font-medium">← Volver</a>
        </div>
      </div>
    </header>

    <main class="max-w-5xl mx-auto px-6 py-8">
      <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="bg-gradient-to-r from-blue-50 to-blue-100 px-8 py-6 border-b border-gray-200">
          <h2 class="text-xl font-bold text-gray-900">Datos del Banco</h2>
        </div>

        <form method="POST" action="/banking" class="p-6 space-y-6">
          <% if (typeof error !== 'undefined' && error) { %>
            <div class="bg-red-50 border border-red-200 text-red-700 p-4 rounded-lg">
              <%= error %>
            </div>
          <% } %>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Nombre del Banco *</label>
              <input name="nombre" type="text" required placeholder="Ej: Banco XYZ"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Código del Banco *</label>
              <input name="codigo_banco" type="text" required placeholder="Ej: 001"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>
          </div>

          <div>
            <label class="block text-sm font-semibold text-gray-700 mb-2">Número de Cuenta Bancaria *</label>
            <input name="cuenta_bancaria" type="text" required placeholder="Ej: 1234567890"
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Tipo de Cuenta *</label>
              <select name="tipo_cuenta" required
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
                <option value="">-- Seleccionar --</option>
                <option value="corriente">Cuenta Corriente</option>
                <option value="ahorros">Cuenta de Ahorros</option>
                <option value="nomina">Cuenta de Nómina</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Saldo Inicial *</label>
              <input name="saldo_inicial" type="number" step="0.01" required placeholder="0.00"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>
          </div>

          <!-- BOTONES DE ACCIÓN -->
          <div class="flex gap-3 justify-between border-t border-gray-200 pt-6">
            <div class="flex gap-3">
              <button type="submit" class="bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700 font-semibold transition flex items-center gap-2 shadow">
                <span>✓</span> Registrar Banco
              </button>
              <a href="/banking" class="bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 font-medium transition flex items-center gap-2 shadow">
                <span>✕</span> Cancelar
              </a>
            </div>
            <a href="/dashboard" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 font-medium transition flex items-center gap-2 shadow">
              <span>🚪</span> Salir
            </a>
          </div>
        </form>
      </div>

      <!-- INFORMACIÓN -->
      <div class="mt-8 bg-yellow-50 border border-yellow-200 rounded-lg p-4">
        <p class="text-sm text-yellow-800">
          <strong>Nota:</strong> Completa todos los campos para registrar una nueva cuenta bancaria. Luego podrás importar estados de cuenta y realizar conciliaciones.
        </p>
      </div>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-5xl mx-auto px-6 py-6 text-center text-sm text-gray-500">
        &copy; <%= new Date().getFullYear() %> Sistema Contable
      </div>
    </footer>
  </body>
</html>
