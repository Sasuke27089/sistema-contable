<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Bancos - Sistema Contable</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 text-gray-800 min-h-screen">
    <!-- HEADER -->
    <header class="bg-white shadow border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Gestión Bancaria</h1>
            <p class="text-sm text-gray-600">Administra tus cuentas bancarias y conciliaciones</p>
          </div>
          <a href="/dashboard" class="text-indigo-600 hover:text-indigo-700 font-medium">← Volver</a>
        </div>
        <nav class="text-sm space-x-4 flex flex-wrap">
          <a href="/banking" class="text-blue-600 font-semibold">Bancos</a>
          <a href="/accounting/cuentas" class="text-gray-600 hover:text-indigo-600">Cuentas</a>
          <a href="/accounting/balance" class="text-gray-600 hover:text-indigo-600">Balance</a>
        </nav>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-6 py-8">
      <!-- BARRA DE ACCIONES -->
      <div class="mb-6 flex justify-between items-center flex-wrap gap-3">
        <div class="flex gap-2 flex-wrap">
          <a href="/banking/crear" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 font-semibold flex items-center gap-2 shadow transition">
            <span>+</span> Nuevo Banco
          </a>
          <button onclick="window.print()" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-700 font-medium flex items-center gap-2 shadow transition">
            <span>🖨️</span> Imprimir
          </button>
        </div>
        <a href="/dashboard" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 font-medium flex items-center gap-2 shadow transition">
          <span>🚪</span> Salir
        </a>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-50 border border-red-200 text-red-700 p-4 rounded-lg mb-6">
          <%= error %>
        </div>
      <% } %>

      <!-- TARJETAS DE BANCOS -->
      <% if (bancos && bancos.length > 0) { %>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          <% bancos.forEach(function(banco) { %>
            <div class="bg-white rounded-lg shadow hover:shadow-lg transition p-6 border-l-4 border-blue-600">
              <div class="flex justify-between items-start mb-4">
                <div>
                  <h3 class="text-lg font-bold text-gray-900"><%= banco.nombre %></h3>
                  <p class="text-sm text-gray-600">Código: <%= banco.codigo_banco %></p>
                </div>
                <span class="bg-blue-100 text-blue-700 px-3 py-1 rounded text-xs font-semibold">
                  <%= banco.tipo_cuenta %>
                </span>
              </div>

              <div class="space-y-2 mb-4 pb-4 border-b border-gray-200">
                <p class="text-sm text-gray-600">
                  <strong>Cuenta:</strong> <%= banco.cuenta_bancaria %>
                </p>
                <p class="text-xl font-bold text-green-600">
                  Saldo: $ <%= banco.saldo_actual.toFixed(2) %>
                </p>
              </div>

              <div class="flex gap-2">
                <a href="/banking/<%= banco.id %>" class="flex-1 bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 text-center font-medium">
                  Ver Detalles
                </a>
                <a href="/banking/<%= banco.id %>/conciliacion" class="flex-1 bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 text-center font-medium">
                  Conciliar
                </a>
              </div>
            </div>
          <% }) %>
        </div>
      <% } else { %>
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-8 text-center">
          <p class="text-blue-700 font-semibold mb-4">No hay bancos registrados</p>
          <a href="/banking/crear" class="inline-block bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700">
            Registrar primer banco
          </a>
        </div>
      <% } %>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-7xl mx-auto px-6 py-6 text-center text-sm text-gray-500">
        &copy; <%= new Date().getFullYear() %> Sistema Contable
      </div>
    </footer>
  </body>
</html>
