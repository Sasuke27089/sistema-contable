<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Importar Movimientos - Sistema Contable</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 text-gray-800 min-h-screen">
    <!-- HEADER -->
    <header class="bg-white shadow border-b border-gray-200">
      <div class="max-w-5xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Importar Movimientos</h1>
            <p class="text-sm text-gray-600">Carga movimientos bancarios desde archivo CSV</p>
          </div>
          <a href="/banking" class="text-indigo-600 hover:text-indigo-700 font-medium">← Volver</a>
        </div>
      </div>
    </header>

    <main class="max-w-5xl mx-auto px-6 py-8">
      <!-- FORMATO CSV -->
      <div class="bg-blue-50 border-l-4 border-blue-500 p-4 rounded mb-8">
        <h3 class="font-bold text-blue-900 mb-2">📋 Formato de CSV Requerido</h3>
        <p class="text-sm text-blue-800 mb-3">El archivo debe estar separado por tuberías (|) con el siguiente formato:</p>
        <code class="block bg-white p-3 rounded border border-blue-200 text-xs overflow-x-auto text-gray-800">
fecha|concepto|descripcion|referencia|tipo|monto<br>
2024-01-15|TRANSFERENCIA|Transfer entrada|REF001|deposito|1500.00<br>
2024-01-16|CHEQUE|Cheque emitido|CHQ123|retiro|500.00
        </code>
        <p class="text-xs text-blue-700 mt-2">
          <strong>Campos:</strong> fecha (YYYY-MM-DD), concepto, descripcion, referencia, tipo (deposito/retiro), monto
        </p>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-50 border border-red-200 text-red-700 p-4 rounded-lg mb-6">
          <%= error %>
        </div>
      <% } %>

      <% if (typeof success !== 'undefined' && success) { %>
        <div class="bg-green-50 border border-green-200 text-green-700 p-4 rounded-lg mb-6">
          <strong>✓ Éxito:</strong> <%= success %>
        </div>
      <% } %>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- UPLOAD FORMA -->
        <div class="bg-white rounded-lg shadow overflow-hidden">
          <div class="bg-gradient-to-r from-green-50 to-green-100 px-6 py-4 border-b border-gray-200">
            <h2 class="text-lg font-bold text-gray-900">Subir Archivo</h2>
          </div>
          <form method="POST" enctype="multipart/form-data" action="/banking/<%= typeof banco_id !== 'undefined' && banco_id ? banco_id : '#' %>/estado-cuenta/<%= typeof estado_cuenta !== 'undefined' && estado_cuenta ? estado_cuenta.id : '#' %>/importar-csv" class="p-6 space-y-4">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Selecciona archivo CSV *</label>
              <input type="file" name="archivo_csv" accept=".csv,.txt" required
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500" />
              <p class="text-xs text-gray-500 mt-1">Solo archivos .csv o .txt</p>
            </div>
            <button type="submit" class="w-full bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 font-semibold transition">
              📤 Subir y Procesar
            </button>
          </form>
        </div>

        <!-- PASTE FORMA -->
        <div class="bg-white rounded-lg shadow overflow-hidden">
          <div class="bg-gradient-to-r from-indigo-50 to-indigo-100 px-6 py-4 border-b border-gray-200">
            <h2 class="text-lg font-bold text-gray-900">Pegar Datos</h2>
          </div>
          <form method="POST" action="/banking/<%= typeof banco_id !== 'undefined' && banco_id ? banco_id : '#' %>/estado-cuenta/<%= typeof estado_cuenta !== 'undefined' && estado_cuenta ? estado_cuenta.id : '#' %>/importar-csv" class="p-6 space-y-4">
            <div>
              <label class="block text-sm font-semibold text-gray-700 mb-2">Pega datos CSV *</label>
              <textarea name="movimientos_csv" rows="8" required placeholder="2024-01-15|TRANSFERENCIA|Transfer entrada|REF001|deposito|1500.00&#10;2024-01-16|CHEQUE|Cheque emitido|CHQ123|retiro|500.00"
                class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 font-mono text-xs"></textarea>
            </div>
            <button type="submit" class="w-full bg-indigo-600 text-white px-6 py-3 rounded-lg hover:bg-indigo-700 font-semibold transition">
              ✓ Procesar Datos
            </button>
          </form>
        </div>
      </div>

      <!-- MUESTRA -->
      <div class="mt-8 bg-white rounded-lg shadow overflow-hidden">
        <div class="bg-gradient-to-r from-gray-50 to-gray-100 px-6 py-4 border-b border-gray-200">
          <h2 class="text-lg font-bold text-gray-900">Descarga Plantilla</h2>
        </div>
        <div class="p-6">
          <p class="text-sm text-gray-600 mb-4">Descarga una plantilla CSV de ejemplo para facilitar la importación:</p>
          <button onclick="descargarPlantilla()" class="bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 font-semibold transition">
            📥 Descargar Plantilla
          </button>
        </div>
      </div>

      <!-- BOTONES DE ACCIÓN -->
      <div class="flex gap-3 justify-between mt-8">
        <a href="/banking/<%= typeof banco_id !== 'undefined' && banco_id ? banco_id : '#' %>/estado-cuenta/<%= typeof estado_cuenta !== 'undefined' && estado_cuenta ? estado_cuenta.id : '#' %>"
          class="bg-gray-600 text-white px-6 py-2 rounded-lg hover:bg-gray-700 font-medium transition flex items-center gap-2 shadow">
          <span>🔙</span> Volver
        </a>
        <a href="/dashboard" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 font-medium transition flex items-center gap-2 shadow">
          <span>🚪</span> Salir
        </a>
      </div>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-5xl mx-auto px-6 py-6 text-center text-sm text-gray-500">
        &copy; <%= new Date().getFullYear() %> Sistema Contable
      </div>
    </footer>

    <script>
      function descargarPlantilla() {
        const plantilla = `fecha|concepto|descripcion|referencia|tipo|monto
2024-01-01|DEPOSITO INICIAL|Apertura de cuenta|APE001|deposito|5000.00
2024-01-05|TRANSFERENCIA|Transfer desde cliente|TRF001|deposito|2000.00
2024-01-10|CHEQUE|Pago de servicios|CHQ001|retiro|150.00
2024-01-15|COMISIONES|Comisión mensual|COM001|retiro|25.00`;
        
        const link = document.createElement('a');
        link.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent(plantilla);
        link.download = 'plantilla-movimientos.csv';
        link.click();
      }
    </script>
  </body>
</html>
