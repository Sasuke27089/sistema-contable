<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Prueba AI - Sistema Contable</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 text-gray-800 min-h-screen">
    <header class="bg-white shadow">
      <div class="max-w-4xl mx-auto px-4 py-4 flex items-center justify-between">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-indigo-600 rounded flex items-center justify-center text-white font-bold">SC</div>
          <div>
            <h1 class="text-lg font-semibold">Sistema Contable</h1>
            <p class="text-sm text-gray-500">Prueba de integración AI</p>
          </div>
        </div>
        <nav class="text-sm">
          <% if (session && session.userId) { %>
            <span class="mr-4">Hola, <%= session.userName %></span>
            <a href="/dashboard" class="mr-3 text-indigo-600">Dashboard</a>
            <a href="/users" class="mr-3 text-indigo-600">Usuarios</a>
            <a href="/logout" class="text-red-500">Cerrar sesión</a>
          <% } else { %>
            <a href="/login" class="mr-3 text-indigo-600">Iniciar sesión</a>
            <a href="/register" class="text-indigo-600">Registro</a>
          <% } %>
        </nav>
      </div>
    </header>

    <main class="max-w-4xl mx-auto p-6">
      <div class="bg-white rounded shadow p-6">
        <h2 class="text-2xl font-semibold mb-4">Prueba AI</h2>
        <form method="POST" action="/ai" class="space-y-4">
          <div>
            <label class="block text-sm font-medium mb-1">Prompt</label>
            <textarea name="prompt" rows="4" class="w-full border px-3 py-2 rounded" required></textarea>
          </div>
          <div>
            <button class="bg-indigo-600 text-white px-4 py-2 rounded">Enviar</button>
          </div>
        </form>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="mt-4 bg-red-50 text-red-700 p-3 rounded"><%= error %></div>
        <% } %>

        <% if (typeof result !== 'undefined' && result) { %>
          <div class="mt-4 bg-gray-900 text-green-50 p-4 rounded whitespace-pre-wrap font-mono text-sm"><%= result %></div>
        <% } %>
      </div>
    </main>

    <footer class="max-w-4xl mx-auto p-6 text-center text-sm text-gray-500">
      &copy; <%= new Date().getFullYear() %> Sistema Contable
    </footer>
  </body>
</html>
