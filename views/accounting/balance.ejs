<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Balance de Prueba - Sistema Contable</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 text-gray-800 min-h-screen">
    <!-- HEADER -->
    <header class="bg-white shadow border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Balance de Prueba</h1>
            <p class="text-sm text-gray-600">Verificación de equilibrio contable (Debe = Haber)</p>
          </div>
          <a href="/dashboard" class="text-indigo-600 hover:text-indigo-700 font-medium">← Volver</a>
        </div>
        <nav class="text-sm space-x-4 flex flex-wrap">
          <a href="/accounting/cuentas" class="text-gray-600 hover:text-indigo-600">Cuentas</a>
          <a href="/accounting/asientos" class="text-gray-600 hover:text-indigo-600">Asientos</a>
          <a href="/accounting/diario" class="text-gray-600 hover:text-indigo-600">Diario</a>
          <a href="/accounting/mayor" class="text-gray-600 hover:text-indigo-600">Mayor</a>
          <a href="/accounting/balance" class="text-red-600 font-semibold">Balance</a>
          <a href="/accounting/resultados" class="text-gray-600 hover:text-indigo-600">Resultados</a>
        </nav>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-6 py-8">
      <!-- BARRA DE ACCIONES -->
      <div class="mb-6 flex justify-between items-center flex-wrap gap-3">
        <div class="flex gap-2">
          <button onclick="window.print()" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-700 font-medium flex items-center gap-2 shadow transition">
            <span>🖨️</span> Imprimir
          </button>
          <button onclick="exportarCSV()" class="bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 font-medium flex items-center gap-2 shadow transition">
            <span>📥</span> Descargar CSV
          </button>
        </div>
        <a href="/dashboard" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 font-medium flex items-center gap-2 shadow transition">
          <span>🚪</span> Salir
        </a>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-50 border border-red-200 text-red-700 p-4 rounded-lg mb-6">
          <%= error %>
        </div>
      <% } %>

      <!-- TABLA PRINCIPAL -->
      <div class="bg-white rounded-lg shadow overflow-hidden">
        <div class="overflow-x-auto">
          <table class="w-full text-sm">
            <thead class="bg-gray-100 border-b border-gray-200">
              <tr>
                <th class="text-left p-4 font-semibold text-gray-700">Código</th>
                <th class="text-left p-4 font-semibold text-gray-700">Cuenta</th>
                <th class="text-left p-4 font-semibold text-gray-700">Tipo</th>
                <th class="text-right p-4 font-semibold text-gray-700">Deudor</th>
                <th class="text-right p-4 font-semibold text-gray-700">Acreedor</th>
              </tr>
            </thead>
            <tbody>
              <% saldos.forEach(function(s, idx) { %>
                <tr class="<%= idx % 2 === 0 ? 'bg-white' : 'bg-gray-50' %> border-b border-gray-200 hover:bg-indigo-50 transition">
                  <td class="p-4 font-mono text-gray-700"><strong><%= s.codigo %></strong></td>
                  <td class="p-4"><%= s.nombre %></td>
                  <td class="p-4">
                    <span class="inline-block px-2 py-1 rounded text-xs font-semibold
                      <%= s.tipo === 'activo' ? 'bg-blue-100 text-blue-700' : 
                          s.tipo === 'pasivo' ? 'bg-red-100 text-red-700' : 
                          s.tipo === 'capital' ? 'bg-purple-100 text-purple-700' : 
                          s.tipo === 'ingreso' ? 'bg-green-100 text-green-700' : 
                          s.tipo === 'gasto' ? 'bg-orange-100 text-orange-700' : 
                          'bg-yellow-100 text-yellow-700' %>">
                      <%= s.tipo %>
                    </span>
                  </td>
                  <td class="text-right p-4 font-mono">
                    <%= (s.total_debe || 0).toFixed(2) %>
                  </td>
                  <td class="text-right p-4 font-mono">
                    <%= (s.total_haber || 0).toFixed(2) %>
                  </td>
                </tr>
              <% }) %>
              
              <!-- FILA DE TOTALES -->
              <tr class="bg-gray-900 text-white font-bold border-t-2 border-gray-300">
                <td colspan="3" class="p-4">TOTALES</td>
                <td class="text-right p-4 font-mono text-lg"><%= totalDebe.toFixed(2) %></td>
                <td class="text-right p-4 font-mono text-lg"><%= totalHaber.toFixed(2) %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <!-- INDICADOR DE EQUILIBRIO -->
      <div class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-4">
        <% 
          const diferencia = Math.abs(totalDebe - totalHaber);
          const equilibrado = diferencia < 0.01;
        %>

        <div class="bg-white rounded-lg shadow p-6">
          <p class="text-sm text-gray-600 mb-1">Total Deudor</p>
          <p class="text-3xl font-bold text-blue-600"><%= totalDebe.toFixed(2) %></p>
        </div>

        <div class="bg-white rounded-lg shadow p-6">
          <p class="text-sm text-gray-600 mb-1">Total Acreedor</p>
          <p class="text-3xl font-bold text-green-600"><%= totalHaber.toFixed(2) %></p>
        </div>

        <div class="<%= (typeof equilibrado !== 'undefined' && equilibrado) ? 'bg-green-50 border-l-4 border-green-500' : 'bg-red-50 border-l-4 border-red-500' %> rounded-lg shadow p-6">
          <p class="text-sm <%= (typeof equilibrado !== 'undefined' && equilibrado) ? 'text-green-600' : 'text-red-600' %> mb-1">Estado</p>
          <p class="text-2xl font-bold <%= (typeof equilibrado !== 'undefined' && equilibrado) ? 'text-green-600' : 'text-red-600' %>">
            <% if (typeof equilibrado !== 'undefined' && equilibrado) { %>
              ✓ Equilibrado
            <% } else { %>
              ⚠ Desbalance: <%= (typeof diferencia !== 'undefined' ? diferencia : 0).toFixed(2) %>
            <% } %>
          </p>
        </div>
      </div>

      <% if (typeof equilibrado !== 'undefined' && equilibrado) { %>
        <div class="mt-6 bg-green-50 border border-green-200 rounded-lg p-4 text-green-700 flex items-center gap-3">
          <span class="text-2xl">✓</span>
          <div>
            <p class="font-semibold">Balance Equilibrado</p>
            <p class="text-sm">La suma total de débitos es igual a la suma total de créditos. El registro contable es correcto.</p>
          </div>
        </div>
      <% } else { %>
        <div class="mt-6 bg-red-50 border border-red-200 rounded-lg p-4 text-red-700 flex items-center gap-3">
          <span class="text-2xl">⚠</span>
          <div>
            <p class="font-semibold">Desbalance Detectado</p>
            <p class="text-sm">Existe una diferencia de <%= diferencia.toFixed(2) %>. Revisa los asientos recientes para corregir.</p>
          </div>
        </div>
      <% } %>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-7xl mx-auto px-6 py-6 text-center text-sm text-gray-500">
        &copy; <%= new Date().getFullYear() %> Sistema Contable
      </div>
    </footer>

    <script>
      function exportarCSV() {
        const tabla = document.querySelector('table');
        const rows = Array.from(tabla.querySelectorAll('tr'));
        const csv = rows.map(row => {
          return Array.from(row.querySelectorAll('td, th'))
            .map(cell => `"${cell.textContent.trim()}"`)
            .join(',');
        }).join('\n');
        
        const link = document.createElement('a');
        link.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent(csv);
        link.download = 'balance_' + new Date().toISOString().split('T')[0] + '.csv';
        link.click();
      }
    </script>
  </body>
</html>
