<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Diario - Sistema Contable</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-50 text-gray-800 min-h-screen">
    <!-- HEADER -->
    <header class="bg-white shadow border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between mb-4">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Libro Diario</h1>
            <p class="text-sm text-gray-600">Registro cronológico de todas las operaciones</p>
          </div>
          <a href="/dashboard" class="text-indigo-600 hover:text-indigo-700 font-medium">← Volver</a>
        </div>
        <nav class="text-sm space-x-4 flex flex-wrap">
          <a href="/accounting/cuentas" class="text-gray-600 hover:text-indigo-600">Cuentas</a>
          <a href="/accounting/asientos" class="text-gray-600 hover:text-indigo-600">Asientos</a>
          <a href="/accounting/diario" class="text-blue-600 font-semibold">Diario</a>
          <a href="/accounting/mayor" class="text-gray-600 hover:text-indigo-600">Mayor</a>
          <a href="/accounting/balance" class="text-gray-600 hover:text-indigo-600">Balance</a>
          <a href="/accounting/resultados" class="text-gray-600 hover:text-indigo-600">Resultados</a>
        </nav>
      </div>
    </header>

    <main class="max-w-7xl mx-auto px-6 py-8">
      <!-- BARRA DE ACCIONES -->
      <div class="mb-6 flex justify-between items-center flex-wrap gap-3">
        <div class="flex gap-2">
          <button onclick="window.print()" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-700 font-medium flex items-center gap-2 shadow transition">
            <span>🖨️</span> Imprimir
          </button>
          <button onclick="exportarCSV()" class="bg-green-600 text-white px-4 py-2 rounded-lg hover:bg-green-700 font-medium flex items-center gap-2 shadow transition">
            <span>📥</span> Descargar CSV
          </button>
        </div>
        <a href="/dashboard" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 font-medium flex items-center gap-2 shadow transition">
          <span>🚪</span> Salir
        </a>
      </div>

      <!-- FILTROS -->
      <div class="bg-white rounded-lg shadow p-6 mb-6">
        <h3 class="font-semibold text-gray-900 mb-4">Filtros</h3>
        <form method="GET" action="/accounting/diario" class="flex gap-2 md:col-span-3 flex-col md:flex-row">
          <div class="flex-1">
            <label class="block text-sm font-medium text-gray-700 mb-2">Desde</label>
            <input type="date" name="fecha_inicio" value="<%= fecha_inicio || '' %>" 
              class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>
          <div class="flex-1">
            <label class="block text-sm font-medium text-gray-700 mb-2">Hasta</label>
            <input type="date" name="fecha_fin" value="<%= fecha_fin || '' %>" 
              class="w-full px-3 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
          </div>
          <div class="flex items-end">
            <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 font-medium">
              Filtrar
            </button>
          </div>
        </form>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-50 border border-red-200 text-red-700 p-4 rounded-lg mb-6">
          <%= error %>
        </div>
      <% } %>

      <!-- ASIENTOS AGRUPADOS POR FECHA -->
      <div class="space-y-6">

        <% if (asientos.length === 0) { %>
          <div class="bg-blue-50 border border-blue-200 rounded-lg p-8 text-center">
            <p class="text-blue-700 font-semibold">No hay asientos en este período</p>
          </div>
        <% } else { %>
          <% 
            let asientosPorFecha = {};
            asientos.forEach(function(a) {
              let fecha = new Date(a.fecha).toLocaleDateString('es-ES');
              if (!asientosPorFecha[fecha]) asientosPorFecha[fecha] = [];
              asientosPorFecha[fecha].push(a);
            });
            let fechas = Object.keys(asientosPorFecha).sort().reverse();
          %>

          <% fechas.forEach(function(fecha) { %>
            <div class="bg-white rounded-lg shadow overflow-hidden">
              <!-- ENCABEZADO DE FECHA -->
              <div class="bg-gradient-to-r from-blue-50 to-blue-100 px-6 py-4 border-l-4 border-blue-600">
                <h3 class="text-lg font-bold text-gray-900"><%= fecha %></h3>
                <p class="text-sm text-gray-600">
                  <%= asientosPorFecha[fecha].length %> asiento(s) registrado(s)
                </p>
              </div>

              <!-- TABLA DE ASIENTOS -->
              <div class="overflow-x-auto">
                <table class="w-full text-sm">
                  <thead class="bg-gray-50 border-b border-gray-200">
                    <tr>
                      <th class="text-left p-4 font-semibold text-gray-700">N° Asiento</th>
                      <th class="text-left p-4 font-semibold text-gray-700">Descripción</th>
                      <th class="text-center p-4 font-semibold text-gray-700">Estado</th>
                      <th class="text-center p-4 font-semibold text-gray-700">Ver</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% asientosPorFecha[fecha].forEach(function(a, idx) { %>
                      <tr class="<%= idx % 2 === 0 ? 'bg-white' : 'bg-gray-50' %> border-b border-gray-200 hover:bg-indigo-50">
                        <td class="p-4">
                          <span class="inline-block bg-blue-600 text-white px-3 py-1 rounded font-bold text-xs">
                            <%= a.numero_asiento %>
                          </span>
                        </td>
                        <td class="p-4 text-gray-700"><%= a.descripcion || '—' %></td>
                        <td class="text-center p-4">
                          <span class="inline-block px-3 py-1 rounded text-xs font-semibold bg-green-100 text-green-700">
                            Registrado
                          </span>
                        </td>
                        <td class="text-center p-4">
                          <a href="/accounting/asientos/<%= a.id %>" class="text-indigo-600 hover:text-indigo-700 font-medium">Ver →</a>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>

      <!-- INFORMACIÓN -->
      <div class="mt-8 bg-yellow-50 border border-yellow-200 rounded-lg p-4">
        <p class="text-sm text-yellow-800">
          <strong>Nota:</strong> El Libro Diario registra todas las operaciones en orden cronológico. 
          Consulta el Libro Mayor para ver el movimiento detallado por cuenta.
        </p>
      </div>
    </main>

    <footer class="bg-white border-t border-gray-200 mt-12">
      <div class="max-w-7xl mx-auto px-6 py-6 text-center text-sm text-gray-500">
        &copy; <%= new Date().getFullYear() %> Sistema Contable
      </div>
    </footer>

    <script>
      function exportarCSV() {
        const tablas = document.querySelectorAll('table');
        let csv = '';
        tablas.forEach((tabla, idx) => {
          if (idx > 0) csv += '\n\n';
          const rows = Array.from(tabla.querySelectorAll('tr'));
          csv += rows.map(row => {
            return Array.from(row.querySelectorAll('td, th'))
              .map(cell => `"${cell.textContent.trim()}"`)
              .join(',');
          }).join('\n');
        });
        
        const link = document.createElement('a');
        link.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent(csv);
        link.download = 'diario_' + new Date().toISOString().split('T')[0] + '.csv';
        link.click();
      }
    </script>
  </body>
</html>
